import createTemplate from '@feat/feat-ui/lib/util/createTemplate';
import './style.scss';

const Username = createTemplate({
  Compo: 'span',
  namespace: 'ft',
  baseName: 'Username',
  displayName: 'UserName',
});

export default Username;
