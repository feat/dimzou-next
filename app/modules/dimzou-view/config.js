export const REDUCER_KEY = 'dimzou-view';

export const initialMetaState = {
  loading: false,
  fetchError: null,
  data: null,
  onceFetched: false,
};

export const initialContentState = {
  loading: false,
  fetchError: null,
  data: null,
  onceFetched: false,
};

// PublicationMetaInfo -- bundleId
// PublicationContent

// 1. step fetch publication meta info
// 2. fetch publication content.
