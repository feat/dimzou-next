import { schema } from 'normalizr';

export const category = new schema.Entity('categories');
export const expCategory = new schema.Entity('expCategories');
export const proCategory = new schema.Entity('proCategories');
